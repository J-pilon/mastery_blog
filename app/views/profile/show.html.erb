<div class="flex flex-col items-center w-full h-full mt-16">
  <div class="flex flex-col items-center mb-12">
    <div class="flex items-center justify-center mb-8 overflow-hidden rounded-full w-80 h-80">
      <img 
        src=<%= profile_image_url(@profile) %> 
        alt="profile image"
        class="object-cover w-full h-full">
    </div>
    
    <h2 class="mb-2"><%= "#{@profile.first_name} #{@profile.last_name}" %></h2>
    
    <%# <p><%= @profile.user.email </p> %>
  </div>

  <div><%= link_to "Edit Profile", edit_profile_path(id: @profile), class: "btn-lg btn-primary" %></div>
  <div><%= button_to "Sign Out", signout_path, class: "btn-lg border-gray border-solid border text-gray mt-8 flex items-center justify-center w-auto font-bold border-2 rounded-xl whitespace-nowrap" %></div>
</div>
